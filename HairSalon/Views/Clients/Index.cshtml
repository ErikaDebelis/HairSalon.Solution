@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model List<HairSalon.Models.Client>;

<h1>Client List</h1>

@if (@Model.Count == 0)
{
  <h3>No clients have been added yet!</h3>
}

<h5>
@foreach (Client client in Model)
{
  <li>Client: @Html.ActionLink($"{client.ClientName}", "Details", new { id = client.ClientId })</li>

  <li>Stylist: @Html.ActionLink($"{client.Stylist.StylistName}", "Details", "Stylists", new {id = client.StylistId})</li>

}
</h5>

<h6>@Html.ActionLink("Home", "Index", "Home")</h6>
<h6>@Html.ActionLink("View all Stylists", "Index")</h6>
<h6>@Html.ActionLink("Add new Client", "Create", "Clients")</h6>
